# Точка продолжения защиты контента — 2026-09-22

## Задача пользователя

По порядку защитить все авторские трактовки и материалы от массового копирования и публикации в исходниках, сохранить сайт/PDF/APK и не переделывать очистку по частям. Лимит сессии заканчивается; сохранять точку продолжения. Не обещать абсолютную защиту или автоматическую отмену ранее предоставленных лицензий.

## Что выполнено

- Стратегия: `docs/CONTENT-PRIVACY-STRATEGY.md`. Это план и ограниченный аудит, не реализованная защита.
- Создан и запущен `scripts/inventory_editorial_content.py`: резервная копия 151 текстового исходного файла и частный реестр. CRC и SHA-256 каждого архивного файла проверены.
- Архив: `data/content_evidence/20260922T052929688291Z/text-originals.zip`.
- Реестр: `data/content_evidence/20260922T052929688291Z/inventory.json`.
- 74 файла отмечены эвристикой для ручного разбора длинного текста. Это не число авторских произведений и не окончательная классификация.
- 7 JSON-файлов `data/` отсутствуют в проверенном исходном снимке: authored_content, authored_transit_content, content_overrides, transit_en/part0..3.
- Архив исключён из Git, подтверждено `git check-ignore`. Он не включает БД, секреты и бинарные изображения/PDF; локальные оригиналы не изменялись.
- До переноса контента выполнены тесты `test_interpretation_access.py`, `test_predictive_interpretations.py`, `test_transit_english.py`: 19 passed. Это исходная контрольная точка, не проверка будущего переноса.

## История и публикация

Новый публичный репозиторий: shubinarti-glitch/astro-calculator-source. Пользователь вручную заменил main на 94a4489f9deda81a0ffa7cb8953be720967363b2, что подтверждал его скриншот forced update. Новый корневой коммит исключает 751 файл frontend/media. Встроенные тексты остались. Старый репозиторий shubinarti-glitch/astro-calculator не очищен. Все удалённые ветки/теги старого репозитория и сторонние копии не проверены.

Локальный исходный репозиторий `.source-publish-20260922`, ветка `codex/clean-public-source`. Локальная main может всё ещё указывать на старый коммит; НЕ публиковать её случайно. Резервная копия прежней истории: `release_backups/public-source-before-history-clean.bundle` (проверена). Автоматическая проверка ранее запретила агенту force-push, пользователь выполнил команду сам. Не обходить отказ.

## Актуальное продолжение — 22 сентября 2026

Этот раздел заменяет устаревшие статусы ниже; нижние записи сохранены как журнал этапов.

- Локально перенесены backend-тексты в `data/editorial/text-snippets-v1.json`, шесть таблиц в `text-tables-v1.json`; структурные HTML/CSS и стабильные ключи CMS оставлены в коде. `backend/editorial_required.json` проверяется при старте.
- Глоссарий перенесён в `glossary-v1.json`; `/api/glossary` возвращает выбранный язык. Проверены RU/EN в браузере на 8033. Старые серверы могут держать прежние модули.
- Android TarotDeck/LunarTexts используют генерируемый Kotlin из частного `android-v1.json`; сеть для доступа к этим текстам не добавлена. Подтверждено точное равенство 78 ID/порядка, 468 полей карт и 40 лунных полей, включая сгенерированный Kotlin.
- Backend: полный прогон до добавления интеграционных тестов — 463 passed, 1 skipped. Повторный целевой прогон загрузчика и интеграции — 41 passed. Сравнение 11364 результатов RU/EN прошло. PDF: 18 тестов прошли; это не визуальная проверка каждого PDF.
- Android: компиляция обоих debug-вариантов прошла, но стандартный Gradle test worker выдаёт ClassNotFoundException для всех тестовых классов на кириллическом пути. Ошибка configuration cache нового генератора исправлена, кэш успешно сохранён. Добавлены задачи printStandardDebugUnitTestClasspath/printGoogleplayDebugUnitTestClasspath для прямого JUnit, аналогично существующему обходу astrocore. Окончательный результат прямого JUnit нужно сверить; проверка устройства/релизного APK не выполнена.
- Публичный экспорт пока только черновик: `ready_to_build=false`, `ready_to_publish=false`. Не публиковать его как полный соответствующий лицензии исходник. Часть ассетов и обязательных уведомлений ещё требует проверки.
- Деплой, push, изменения БД и удаление истории в этом этапе НЕ выполнялись. Приватные пакеты обязательны для нового запуска/сборки и не должны попадать в публичный Git.

Дополнение: прямой JUnit успешно выполнил все 27 тестов для StandardDebug и все 27 для GoogleplayDebug. Использован фактический classpath соответствующих Gradle-задач, без исключения тестов. Это подтверждает JVM-проверки, но не работу на устройстве.

В `_brief_report` исправлена выдача полных альтернативных трактовок: `interp_sign`, `interp_house`, `interp_plain` и `interp_full_plain` теперь сокращаются только в brief-режиме. Схема полей и расчётные положения сохранены. Добавлены регрессионные тесты сохранности исходного объекта и полного доступа подписчика; тест ограничения воспроизвёл ошибку до исправления.

Остаётся: закончить безопасную упаковку публичных исходников с учётом лицензий, проверить APK на устройстве и подготовить точный состав релиза. Офлайн-текст по-прежнему извлекаем из APK; абсолютная защита от копирования не обещается.

### Продолжение — 23 сентября 2026

- Свежие StandardDebug и GoogleplayDebug APK успешно собраны. Debug получил суффикс `ru.astrosmap.app.debug`, поэтому устанавливается рядом с боевым приложением и не затрагивает его данные.
- StandardDebug установлен на эмулятор. MainActivity запустилась, главный экран открылся, лунные тексты из сгенерированного приватного пакета отображаются; в logcat нет FATAL EXCEPTION/AndroidRuntime для приложения.
- Добавлены официальные уведомления html2pdf.js 0.10.1 и Gradle 8.13. Экспорт сообщает `missing_notices=[]` и `missing_build_inputs=[]`.
- Экспортёр теперь исключает и только по имени фиксирует локальные `android/*.jks`, `android/signing.properties`, `android/local.properties`, не читая и не копируя их. Его 18 тестов прошли, 1 симлинк-тест пропущен средой.
- Создан локальный чистый корневой снимок `.source-protected-20260923`, коммит `6c463f7f6adcb78ec3569b4d850044fb87586eae`. В нём 243 файла; отсутствуют data/media/store/assets, БД, ключи, APK/AAB и проверенная фраза из приватной колоды. Сравнение 911 длинных значений приватных пакетов нашло только 5 технических/структурных совпадений: два сообщения о некорректном локальном времени, HTML-каркас, XML-заголовок sitemap и тест сообщения времени. Авторских трактовок среди совпадений нет.
- Старый публичный GitHub всё ещё содержит встроенные трактовки. Новый снимок не отправлен: замена публичной истории требует ручного force-push владельца по `AGENTS.md`. Отчёт продолжает честно ставить `ready_to_build=false`/`ready_to_publish=false`, потому что приватные runtime-пакеты и часть ресурсов исключены.
- На свежем локальном сервере 8034 браузер открыл сайт и выполнил реальный пример натальной карты; расчёт и краткие трактовки отобразились. Целевой прогон: 30 passed, 1 skipped; ранее API: 87 passed; Android JUnit: 27/27 для обоих вариантов.

## Исторические записи этапов

### Обновление: первый перенос выполнен локально

- `backend/editorial_data.py`: загрузчик JSON-данных с восстановлением tuple и ключей dict без eval.
- Шесть таблиц из `transit_english.py` (ASPECTS/TIMING/ROLES) и `vedic.py` (NAKSHATRA_GUIDE/_PAKSHA_ADVICE/_DAY_ADVICE) перенесены в **приватный** `data/editorial/text-tables-v1.json`. Файл обязателен для запуска изменённого backend. Не публиковать/не деплоить изменённый код без доставки этого пакета на сервер и проверки запуска. Подготовка публичной демонстрационной сборки ещё не выполнена.
- Миграция: `scripts/extract_editorial_tables.py` (одноразовая, повторный запуск защищён от перезаписи пакета).
- Снимок до изменения: `data/content_evidence/pre-extraction-extended.json`. 11364 результатов RU/EN (включая транзиты и календарь) совпали после переноса.
- 101 тест прошёл: test_api, test_interpretation_access, test_predictive_interpretations, test_transit_english. После косметической замены импортов ещё раз совпали 11364 выхода.
- Новый backend запущен на 8032, реальный POST /api/vedic-calendar вернул 200. Старый 8031 может содержать прежние импортированные модули.
- Android не изменён. Остальные встроенные тексты пока не перенесены. Утверждения ниже «перенос не начат» относятся к прежнему этапу, см. это обновление.
- Пользователь явно разрешил субагентов: Locke (01a0c8ad-834e-7f52-b014-d61a6926d816) аудит Android, Turing (01a0c8ad-862d-7532-990d-cf44ec12e520) тесты только tests/test_editorial_data.py. Получить результаты перед передачей следующего этапа.

Результаты помощников получены: Turing добавил `tests/test_editorial_data.py`, 34 теста прошли (временные данные, кеш очищается, реальные данные не затрагиваются). Locke подтвердил зависимости TarotDeck/LunarTexts от TodayScreen, ассистента, WidgetUpdateWorker и DailyWorker: нельзя делать загрузку зависящей от Activity/сети. Предлагаемый вариант сохранения офлайн — приватный пакет при сборке, генерируемые Kotlin-данные с прежними фасадами. Это НЕ предотвращает извлечение из APK. Сохранять 78 ID/порядок, все 468 RU/EN полей карт, фазы/советы/настроения, выбор языка при обращении. Проверить холодный запуск workers, отсутствие сети, сохранённые расклады, обе store-сборки. APK пока не изменён; заключение помощника статическое.

Дополнение после продолжения: создана `docs/CONTENT-SOURCE-MAP.md` с классификацией источников и каналов выдачи. Скрипт `scripts/content_regression_snapshot.py` сохранил 2520 отпечатков RU/EN в `data/content_evidence/pre-extraction-output.json`; повторное сравнение прошло. Существующий capture не перезаписывать: использовать compare после изменений. Выявлены отдельные Android источники TarotDeck.kt (78 карт) и LunarTexts.kt. Перенос ещё не начат; короткие строки, весь мультимедийный контент и полное покрытие отчётов требуют продолжения.

Не перенесены встроенные Python/JS/Kotlin трактовки в приватный пакет; не изменены правила API, APK, PDF или офлайн-режим; не оформлены окончательные условия на самостоятельный контент; не проверены полностью метаданные; текущая работа не опубликована.

## Следующая работа: завершить пункт 1, затем пункт 2

1. Прочитать реестр и разобрать 74 кандидата: независимые трактовки; шаблоны/f-строки; технические ключи; UI-подписи; комментарии. Добавить отдельную инвентаризацию изображений/PDF, не стирать оригинальные метаданные.
2. Составить карту вызовов Python (`interpretations`, `transit_english`, `vedic`), JS (глоссарий и др.), Kotlin (`LunarTexts`, `TarotDeck` и др.), SEO и PDF. `ChartTexts.kt` главным образом парсит серверный ответ, не считать весь файл авторским текстом.
3. До преобразования зафиксировать детерминированные примеры RU/EN, все типы отчётов/прав доступа. Сохранить ключи CMS. Не применять слепую замену всех строк.
4. Проектировать приватный редакционный пакет и нейтральные демонстрационные данные с учётом комплектности исходников и прошлых лицензий. Юридическую независимость не считать доказанной одним переносом.
5. APK: офлайн-тексты извлекаются из пакета. Пользователь ещё НЕ выбирал отказ от офлайн-трактовок. Не менять это поведение без решения.
6. Только после полного переноса и тестов готовить единый новый исходный снимок и дальнейшую очистку истории.

## Остальные локальные изменения

`frontend/index.html` и `frontend/js/i18n.js` содержат ранее подготовленное сокращение подписи исходников и новый URL; не задеплоены. Есть `scripts/check_public_source.py`, `scripts/content_fingerprint.py`, документация. Рабочее дерево содержит много прежних неотслеживаемых артефактов; не добавлять их массово и не удалять. Сервис uvicorn, запущенный ранее на 8031, мог остаться активным; проверить перед повторным запуском.
