# Передача дел: сайт astrosmap.ru

Рабочая справка по сайту-астрокалькулятору. Дополняет `README.md` (возможности/API),
`ДЕПЛОЙ.md` (пошаговый деплой) и `CLAUDE.md` (правила работы). Актуально на 01.08.2026.

---

## 1. Что это и стек

Веб-сайт для расчёта натальных карт, транзитов, синастрии, соляра/лунара, прогрессий,
ректификации (аналог geocult.ru). Тёмная космическая тема, мультиязычность RU/EN.

- **Бэкенд:** Python + FastAPI (`backend/`), расчёты — Kerykeion / Swiss Ephemeris (режим Moshier),
  БД — SQLite (`data/app.db`).
- **Фронтенд:** ванильный JS + HTML/CSS (`frontend/`), i18n в `frontend/js/i18n.js`.
- **Авторизация:** Bearer-токен (не cookie), лимитер логина/регистрации в памяти.

---

## 2. Локальный запуск

```bash
python run.py           # → http://127.0.0.1:8000
```
Или `Запустить сайт.bat`. Превью-конфиг — `.claude/launch.json` (имя `astro`, порт 8000).
С Android-эмулятора локальный бэкенд виден как `10.0.2.2:8000`.

**Тесты бэкенда:** `pytest` (см. `tests/test_api.py`). Перед сдачей правок бэкенда — прогнать.

---

## 3. Прод-сервер

- **VPS SpaceWeb** `77.222.42.168` (СПб, ЦОД ОБИТ, Ubuntu 24.04, 2CPU/2ГБ/30ГБ, ~788 ₽/мес).
  Переезд с Timeweb был из-за сетевой фильтрации входящего трафика.
- **Каталог:** `/opt/astro` (git-репо). Внутри: `venv`, `data/`, `frontend/`, `backend/`.
- **Сервис:** systemd `astro` (uvicorn на `127.0.0.1:8000 --proxy-headers`).
- **Прокси/HTTPS:** Caddy (`/etc/caddy/Caddyfile`, `astrosmap.ru`+`www` → 8000, сертификат Let's Encrypt).
- **Бэкап БД:** cron в 4:00 копирует `app.db`.
- **APK приложения** раздаётся статикой: `/opt/astro/frontend/astrosmap.apk` → https://astrosmap.ru/astrosmap.apk

---

## 4. Деплой (⚠️ формально зона человека — см. CLAUDE.md)

SSH-ключ у Claude: **`C:\Users\archi\.ssh\astro_key`** (pub в `authorized_keys` root).

```bash
ssh -i ~/.ssh/astro_key root@77.222.42.168 "cd /opt/astro && git pull && systemctl restart astro"
```
- Для чисто **статических** правок (`frontend/`, APK) рестарт не нужен.
- **Деплой только ff-only.** ⚠️ Бывает **вторая параллельная сессия Claude**, пушащая в тот же
  `main` — риск затирания. Перед push/pull сверять `git log`, не делать `--force`.
- На сервере держать бэкап перед ручными правками (были `main.py.bak`, `main.py.pre-deploy.bak`).
- Проверка после деплоя: `curl` главной, `/sitemap.xml`, `POST /api/natal` — должны отдавать 200/расчёт.

---

## 5. Секреты и данные (СТОП — зона человека)

Необратимое, руками владельца, Claude готовит но не выполняет:
- `data/secret.key` — боевой ключ шифрования. Не трогать, не коммитить.
- `data/app.db` — боевая БД (пользователи, платежи). Миграции/удаления — только вручную.
- `data/yookassa.json` — ключи ЮKassa. Боевые ключи заменяются по SSH + рестарт `astro`.
- Массовые рассылки, `git push --force`, `rm -rf` — не делать.

Пользователь `archi.furr@gmail.com` = админ (id 1), премиум до 2036 (вставлен в БД вручную).

---

## 6. Важные технические моменты

- **Эфемериды на Windows:** Swiss Ephemeris не читает `.se1` по кириллическим путям.
  `backend/ephe.py` автоматически копирует файлы в ASCII-папку (`~/.astro_ephe` / `C:\sweph`). Не трогать без нужды.
- **XSS:** ввод пользователя (имя/город) попадает в SVG и HTML — разметка вырезается в
  `main.py` (`_strip_markup`). Защиту сохранять.
- **CSP:** заголовки в middleware `_sync_content` (`main.py`, константа `CSP_POLICY`).
  `script-src` разрешает инлайн-скрипт темы (`index.html`) по **sha256-хэшу** — при правке
  этого скрипта пересчитать хэш, иначе переключение темы сломается. `'unsafe-inline'` в `script-src` не добавлять.
- **Тексты трактовок** редактируются через админку (`content_store.py` + `content_overrides.json`),
  синхронизация по mtime. Ключи текстов не переименовывать бездумно — сломает оверрайды.
- **Авторские трактовки** (знак+дом, RU+EN) — моат продукта, не зашиваются в APK, только с сервера.
  Стиль текстов: без шаблонных зачинов, ротация палитры связок; всегда проверять русские падежи (склонять знаки).
- **Защита от копирования** (базовые деттеренты): `user-select:none` + блок copy/cut/contextmenu
  в `#results` и `#daily-block`; формы не тронуты. Отпугивает, но обходимо.

---

## 7. Монетизация и правовое

- **Подписка «Премиум»:** фримиум-модель (бесплатно 1 карта + деградация), 299 ₽/мес и 1990 ₽/год,
  через ЮKassa. Код готов, **ждёт боевых ключей ЮKassa**.
- **СБП** подключён к боевому магазину 1405218 (лого СБП в подвал не нужно).
- **Возрастной рейтинг 18+** везде.
- **152-ФЗ:** политика/соглашение/чекбоксы согласия/пометка 18+ — в коде. Оператор ПД:
  Шубин А.И., самозанятый, ИНН 245010711934.
- **РКН:** уведомление об обработке ПД подано 12.07.2026 через ЕСИА, ждём включения в реестр.
- **Лицензия кода:** AGPL + подписка (тексты = моат). Правовые страницы:
  https://astrosmap.ru/privacy.html, https://astrosmap.ru/terms.html

---

## 8. SEO и админка

- **240 SEO-страниц** трактовок + `sitemap.xml`/`robots.txt` готовы. Инструкция — в `ДЕПЛОЙ.md`.
- Подтверждены Яндекс.Вебмастер и Google Search Console, sitemap отправлен в оба.
- **Админка** (`/`, вкладки Обзор/Пользователи/Платежи/Тексты): выдача/снятие премиума,
  бан/разбан, таблица платежей и доходов, usage_stats по функциям, смена паролей.

---

## 9. Домен и почта (не трогать)

- Домен `astrosmap.ru` зарегистрирован в **Timeweb** (оплачен до 09.07.2027, автопродление вкл).
  A-записи `@` и `www` → `77.222.42.168`. **MX/TXT почтовые записи не трогать.**
- Почта проекта: `astrosmap@yandex.ru` (был антиспам-блок внешних писем).
  Для тестов слать на `astrosmap@gmail.com` / `archi.furr@gmail.com` (НЕ `archifyrr@gmail.com`).

---

## Связанные файлы и память

- `README.md` — возможности, таблица API, архитектура.
- `ДЕПЛОЙ.md` — пошаговый деплой, шпаргалка РКН.
- `android/ПЕРЕДАЧА-ANDROID.md` — всё про APK и Android-сборку.
- Память (авто, между сессиями): `deploy-status`, `billing-premium`, `legal-compliance`,
  `seo-pages`, `cabinet-v1`, `yandex-mail-status`, `parallel-session-git-risk`.
