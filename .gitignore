# Закрытый контент-слой и данные (НЕ публикуются: БД, ключи, авторские тексты).
# Слэш в начале обязателен: без него правило совпадало с ЛЮБОЙ папкой data/ на любой
# глубине и прятало от git весь data-слой Android-приложения (Room, API, синхронизация).
/data/

# Внутренние рабочие файлы (не для публичного репозитория)
.claude/
CLAUDE.md
ДЕПЛОЙ.md
ДЕПЛОЙ.pdf

# Окружение и мусор
venv/
__pycache__/
*.pyc
.pytest_cache/
